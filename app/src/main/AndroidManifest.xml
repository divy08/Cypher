<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.insane.cypher">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cypher">
        <activity android:name=".StudentTimetableActivity"></activity>
        <activity android:name=".StudentHomeActivity" />
        <activity android:name=".dataclass.StudentHomeActivity" />
        <activity
            android:name=".PopupTimetableActivity"
            android:label="Import Time Table"
            android:theme="@style/popUpTT" />
        <activity android:name=".TimetableActivity" />
        <activity android:name=".FacultyHomeActivity" />
        <activity android:name=".FacultyRegisterActivity" />
        <activity android:name=".StudentRegisterActivity" />
        <activity android:name=".StudentLoginActivity" />
        <activity android:name=".FacultyLoginActivity" />
        <activity android:name=".HostActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>